# 🧮 Calculator CLI — C Programming

A simple **Command Line Calculator** built in **C**, designed to practice control flow, loops, user input handling, and basic arithmetic operations.

This project runs entirely in the terminal (CLI) and allows the user to perform multiple calculations in a continuous loop until they choose to exit.

---

## 🚀 Features

- Interactive CLI menu
- Four basic operations:
  - ➕ Addition
  - ➖ Subtraction
  - ✖️ Multiplication
  - ➗ Division
- Loop-based execution (no restart needed)
- Floating-point number support
- Simple and clear user interaction

---

## 🧠 Concepts Practiced

- `while` loops
- `if` conditional statements
- User input with `scanf`
- Output formatting with `printf`
- Variables and basic program structure in C
- CLI-based application logic

---

## 🛠️ How to Compile and Run

Make sure you have **GCC** installed.

```bash
gcc calculator.c -o calculator
./calculator
